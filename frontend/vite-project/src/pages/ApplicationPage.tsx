import ApplicationForm from "../components/ApplicationForm"; 

/**
 * Renders the application page.
 * This page contains the {@link ApplicationForm} component, 
 * which allows users to submit their applications.
 *
 * @returns The rendered ApplicationPage component.
 */
export default function ApplicationPage() {
  return <ApplicationForm />;
}